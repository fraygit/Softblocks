
<section class="content">
    <div class="container-fluid">


        <div class="row">
            <div class="col-lg-12">
                <div class="view-header">
                    <div class="header-icon">
                        <i class="pe page-header-icon pe-7s-note2"></i>
                    </div>
                    <div class="header-title">
                        <h3>Organisation</h3>
                    </div>
                </div>
                <hr>
            </div>
        </div>

        <div class="row">

            <div class="col-md-6">
                <div class="panel panel-filled">
                    <div class="panel-heading">
                        Create an Organisation
                    </div>
                    <div class="panel-body">

                        <p>Individual form controls automatically receive some global styling.</p>

                        <form id="frmCreateOrg">
                            <div class="form-group">
                                <label for="exampleInputName">Organisation Name</label> 
                                <input type="text" class="form-control" id="txtOrganisationName" placeholder="Name" required="">
                            </div>
                            <div class="form-group">
                                <label for="exampleInputEmail1">Description</label> 
                                <textarea class="form-control" rows="3" placeholder="Textarea example text" id="txtDescription"></textarea>
                            </div>
                            <!--<button type="button" class="btn btn-default" id="btnCreate">Create</button>-->
                            <button type="button" class="btn btn-default" id="btnCreate">Create and go to Organisation</button>
                        </form>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

@section Scripts{
    <script type="text/javascript">
        $(function () {
            $("#btnCreate").click(function () {
                var isValid = $("#frmCreateOrg").valid();
                if (isValid) {
                    var data = {
                        Name: $("#txtOrganisationName").val(),
                        Description: $("#txtDescription").val()
                    }
                    $.ajax({
                        type: "POST",
                        url: "/Organisation/Create",
                        contentType: "application/json",
                        dataType: "json",
                        data: JSON.stringify(data),
                        success: function (d) {
                            if (d.IsSuccess == true) {
                                $("#frmCreateOrg")[0].reset();
                                toastr.success('Successfully created organisation!');
                                setTimeout(function () {
                                    document.location.href = "/";
                                }, 2000);
                            }
                            else {
                                toastr.warning('Error occured. ' + d.Message);
                            }
                        },
                        error: function (xhr, textStatus, errorThrown) {
                            // TODO: Show error
                        }
                    });
                }
            });
        });
    </script>
}